import React from 'react';

class Transformations extends React.Component {
  render() {
    return (
      <div>
        THIS IS Transformations PAGE
        <button onClick={() => console.log('thisi s test')}>test</button>
      </div>
    );
  }
}

export default Transformations;
